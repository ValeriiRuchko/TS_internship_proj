import { Box, Typography } from "@mui/material";
import { observer } from "mobx-react-lite";

export const CreateAndUpdateMed = observer(() => {
  return (
    <Box className="main_content">
      <Typography variant="h1">Changing med</Typography>
    </Box>
  );
});
